/** @type {import('tailwindcss').Config} */
export default {
  content: ["./src/**/*.{html,js,jsx,ts,tsx}"], // Include HTML, JavaScript, and TypeScript files
  theme: {
    extend: {}, // Optionally extend Tailwind CSS theme
  },
  plugins: [], // Optionally add Tailwind CSS plugins
}
